export const validPhone = new RegExp(`^[0-9]{10}$`); // eslint-disable-line
export const validEmail = new RegExp(
    `^[a-zA-Z0-9._:$!%-]+@[a-zA-Z0-9.-]+.[a-zA-Z]$` // eslint-disable-line
);
export const validZip = new RegExp(`^[0-9]*$`); // eslint-disable-line
